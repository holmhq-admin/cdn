import t from"https://cdn.jsdelivr.net/npm/pica@9/+esm";const e=new t;export async function compressImage(t,{maxBytes:a=25e4,maxDim:i=1536}={}){const m=await createImageBitmap(t),n=Math.min(1,i/Math.max(m.width,m.height)),o=document.createElement("canvas");o.width=Math.max(1,Math.round(m.width*n)),o.height=Math.max(1,Math.round(m.height*n)),await e.resize(m,o);for(const t of[.85,.75,.65,.55,.5]){const i=await e.toBlob(o,"image/jpeg",t);if(i.size<=a)return i}return e.toBlob(o,"image/jpeg",.5)}